import { ImagenEmtyPipe } from './imagen-emty.pipe';

describe('ImagenEmtyPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagenEmtyPipe();
    expect(pipe).toBeTruthy();
  });
});
